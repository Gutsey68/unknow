{
  "name": "bun",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:remove": "docker compose down --volumes",
    "seed": "bun run --cwd apps/api seed",
    "drizzle:studio": "cd apps/api && drizzle-kit studio"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
